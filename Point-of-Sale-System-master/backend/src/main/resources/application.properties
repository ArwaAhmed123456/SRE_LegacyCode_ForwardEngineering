# MongoDB connection (override with SPRING_DATA_MONGODB_URI env var)
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/posdb}

server.port=8080

# Logging
logging.level.org.springframework=INFO
